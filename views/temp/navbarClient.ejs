
<nav class="navbar">
  <div class="container nav-wrapper">
    <a href="#" class="logo">Studio<span>Books</span></a>
    <div class="menu-wrapper">
      <ul class="menu">
        <li class="menu-item"><a href="#" class="menu-link active">Beranda</a></li>
        <li class="menu-item"><a href="/studios" class="menu-link">Studio</a></li>
        <li class="menu-item"><a href="#" class="menu-link">Hubungi Kami</a></li>
        <li class="menu-item"><a href="#" class="menu-link">Riwayat Sewa Studio</a></li>
      </ul>
      <% if(!user) {%>
        <a href="/auth/login" class="btn-login">Login</a>
      <%} else { %>
        <% if (user.role === "OWNER") { %>
          <div id="dropdown" class="dropdown">
            <button class="dropdown-toggle" id="dropdownButton">Hello, <%= user.username %></button>
            <div class="dropdown-menu" id="dropdownMenu">
              <a href="/dashboard" class="dropdown-item"><i class="bx bx-user"></i>My dashboard</a>
              <a href="#" class="dropdown-item"><i class="bx bx-cog"></i>Profile Setting</a>
              <a href="/logout" class="dropdown-item"><i class="bx bx-log-out"></i>Log Out</a>
            </div>
          </div>
          <!-- <a href="/dashboard" class="btn-login">My dashboard</a>
          <a href="/logout" class="btn-login">Logout</a> -->
        <% } else if (user.role === "CLIENT") { %>
          <!-- <a href="#" class="btn-login">Hello, <%= user.username %></a>
          <a href="/logout" class="btn-login">Logout</a> -->
          <div id="dropdown" class="dropdown">
            <button class="dropdown-toggle" id="dropdownButton">Hello, <%= user.username %></button>
            <div class="dropdown-menu" id="dropdownMenu">
              <!-- <a href="/dashboard" class="dropdown-item"><i class="bx bx-user"></i>My dashboard</a> -->
              <!-- <a href="#" class="dropdown-item"><i class="bx bx-cog"></i>Profile Setting</a> -->
              <a href="/logout" class="dropdown-item"><i class="bx bx-log-out"></i>Log Out</a>
            </div>
          </div>
        <% } %>
      <% } %>
    </div>
    <div class="menu-toggle bx bxs-grid"></div>
  </div>
</nav>